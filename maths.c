#include<stdio.h>
#include<conio.h>
int main()
        {
                int a,b,sum,sub,multi,divi;
                a=55;
                b=5;
                sum=a+b;
                sub=a-b;
                multi=a*b;
                divi=a/b;
                printf("a=%d b=%d",a,b);
                printf("sum=%d",sum);
                printf("sub=%d",sub);
                printf("multi=%d",multi);
                printf("divi=%d",divi);
        }